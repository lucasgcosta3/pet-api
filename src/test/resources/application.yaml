spring:
  datasource:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  docker:
    compose:
      enabled: false

google:
  workspace:
    credentials-file-path: ""
    sheets:
      spreadsheet-id: ""
